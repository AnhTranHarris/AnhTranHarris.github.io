name: Card swipe rebuild archive
on:
  workflow_dispatch:
permissions:
  contents: read
jobs:
  note:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Card swipe rebuild is maintained directly in index.html."
