/* Harris Portfolio intro — particle streak/glint erasure reveal. */
(() => {
'use strict';
const overlay=document.getElementById('portfolio-entry-overlay');if(!overlay)return;
const nav=performance.getEntriesByType?.('navigation')?.[0];
const skip=nav?.type==='back_forward'||window.matchMedia?.('(forced-colors: active)').matches||window.matchMedia?.('(prefers-reduced-motion: reduce)').matches;
const finishImmediately=()=>{document.documentElement.dataset.entryState='complete';overlay.remove();};if(skip){finishImmediately();return;}
const TOTAL_MS=3200,EFFECT_FADE_START=1600,WHITE_GONE_AT=2400,FAILSAFE_MS=4300;
const COLORS={darkTeal:'#06151c',tealDark:'#0b4d50',tealGreen:'#1a7f77',tealBright:'#63d5d0',tealWhite:'#edf7f8',gold:'#d8b86a',goldHi:'#fff0b0'};
const rand=(a,b)=>a+Math.random()*(b-a),clamp=(v,a,b)=>Math.max(a,Math.min(b,v));
const easeInOut=t=>{t=clamp(t,0,1);return t<.5?4*t*t*t:1-Math.pow(-2*t+2,3)/2;};
const tealLayer=document.createElement('div');tealLayer.className='entry-teal-layer';tealLayer.setAttribute('aria-hidden','true');
const mask=document.createElement('canvas');mask.className='entry-mask-canvas';mask.setAttribute('aria-hidden','true');
const fx=document.createElement('canvas');fx.className='entry-fx-canvas';fx.setAttribute('aria-hidden','true');overlay.append(tealLayer,mask,fx);
const mctx=mask.getContext('2d',{alpha:true,desynchronized:true}),fctx=fx.getContext('2d',{alpha:true,desynchronized:true});if(!mctx||!fctx){finishImmediately();return;}
let cssW=1,cssH=1,dpr=1,start=0,raf=0,watchdog=0,finished=false,tealEvents=[],goldEvents=[];
function resizeCanvas(c,ctx){c.width=Math.max(1,Math.round(cssW*dpr));c.height=Math.max(1,Math.round(cssH*dpr));c.style.width=`${cssW}px`;c.style.height=`${cssH}px`;ctx.setTransform(dpr,0,0,dpr,0,0);ctx.imageSmoothingEnabled=true;}
function makeParticle(o={}){const thickness=o.thickness??(Math.random()<.84?rand(.55,2.1):rand(2.1,3)),dir=o.dir??(Math.random()<.5?1:-1),family=Math.random(),color=family<.42?COLORS.tealDark:family<.84?COLORS.tealGreen:COLORS.tealBright,headLen=o.headLen??rand(2.5,13);return{y:o.y??rand(.5,Math.max(1,cssH-.5)),thickness,headLen,tailLen:o.tailLen??50*cssW,dir,start:o.start??rand(25,WHITE_GONE_AT-220),duration:o.duration??rand(105,245),color,intensity:o.intensity??rand(.5,1),halo:o.halo??rand(.28,.88),eraseWidth:o.eraseWidth??Math.max(1,thickness*rand(.95,1.6)),lastHead:null,coverage:!!o.coverage,completed:false};}
function buildEvents(){tealEvents=[];goldEvents=[];const independentCount=Math.round(rand(184,276));for(let i=0;i<independentCount;i++)tealEvents.push(makeParticle());const clusterCount=Math.round(rand(10,18));for(let c=0;c<clusterCount;c++){const members=Math.round(rand(4,9)),dir=Math.random()<.5?1:-1,baseY=rand(cssH*.05,cssH*.95),baseStart=rand(80,WHITE_GONE_AT-420),baseDuration=rand(120,225),spread=rand(5,28);for(let j=0;j<members;j++)tealEvents.push(makeParticle({y:clamp(baseY+rand(-spread,spread),.5,cssH-.5),dir,start:baseStart+rand(-22,38),duration:baseDuration*rand(.88,1.14),tailLen:50*cssW,intensity:rand(.62,1),halo:rand(.35,.92)}));}const laneH=clamp(cssH/72,5,12),lanes=Math.ceil(cssH/laneH);for(let i=0;i<lanes;i++){const top=i*laneH,h=Math.min(laneH+1,cssH-top+1),y=clamp(top+h*.5+rand(-h*.18,h*.18),.5,cssH-.5);tealEvents.push(makeParticle({y,dir:i%2===0?1:-1,start:rand(120,2050),duration:rand(125,230),tailLen:50*cssW,thickness:rand(.7,1.9),eraseWidth:h+1,intensity:rand(.42,.78),halo:rand(.22,.58),coverage:true}));}const goldCount=Math.round(rand(3,6));for(let i=0;i<goldCount;i++)goldEvents.push({y:rand(cssH*.08,cssH*.92),thickness:rand(7,14),dir:Math.random()<.5?1:-1,start:rand(30,260),duration:rand(2100,2450),phase:rand(0,Math.PI*2),baseAlpha:rand(.18,.34),glintWidth:rand(.10,.23)*cssW});}
function resetWhite(){mctx.globalCompositeOperation='source-over';mctx.globalAlpha=1;mctx.fillStyle='#fff';mctx.fillRect(0,0,cssW,cssH);}
function resize(){cssW=Math.max(1,innerWidth);cssH=Math.max(1,innerHeight);dpr=Math.min(devicePixelRatio||1,3);resizeCanvas(mask,mctx);resizeCanvas(fx,fctx);tealLayer.style.background=COLORS.darkTeal;tealLayer.style.opacity='1';resetWhite();buildEvents();}
function eraseRect(x,y,w,h,a=1){if(w<=0||h<=0)return;mctx.save();mctx.globalCompositeOperation='destination-out';mctx.globalAlpha=a;mctx.fillStyle='#000';mctx.fillRect(x,y,w,h);mctx.restore();}
function eraseParticlePath(e,head){const prev=e.lastHead;if(prev!==null)eraseRect(Math.min(prev,head)-e.headLen*.35,e.y-e.eraseWidth/2,Math.abs(head-prev)+e.headLen*.7,e.eraseWidth,1);else eraseRect(head-e.headLen*.5,e.y-e.eraseWidth/2,e.headLen,e.eraseWidth,1);e.lastHead=head;}
function completeCoverage(e){if(!e.coverage||e.completed)return;eraseRect(0,e.y-e.eraseWidth/2,cssW,e.eraseWidth,1);e.completed=true;}
function drawTealEvent(e,t,a){const local=(t-e.start)/e.duration;if(local<0){e.lastHead=null;return;}if(local>1){completeCoverage(e);e.lastHead=null;return;}const headPad=Math.max(24,e.headLen*2),travel=cssW+headPad*2,head=e.dir>0?-headPad+local*travel:cssW+headPad-local*travel;eraseParticlePath(e,head);const tailStart=e.dir>0?head-e.tailLen:head+e.tailLen,g=fctx.createLinearGradient(tailStart,0,head,0);
// 50x viewport tail: far tip fades briefly, then ~95% stays visibly solid before the hot head.
g.addColorStop(0,'rgba(6,21,28,0)');
g.addColorStop(.01,'rgba(11,77,80,.18)');
g.addColorStop(.03,'rgba(11,77,80,.44)');
g.addColorStop(.05,'rgba(11,77,80,.56)');
g.addColorStop(.25,'rgba(11,77,80,.58)');
g.addColorStop(.50,'rgba(26,127,119,.60)');
g.addColorStop(.75,'rgba(26,127,119,.62)');
g.addColorStop(.95,'rgba(26,127,119,.66)');
g.addColorStop(.98,'rgba(99,213,208,.80)');
g.addColorStop(.995,'rgba(99,213,208,.94)');
g.addColorStop(1,'rgba(237,247,248,1)');
fctx.save();fctx.globalAlpha=a*e.intensity*.44*e.halo;fctx.strokeStyle=g;fctx.lineWidth=Math.max(.45,e.thickness*1.75);fctx.lineCap='butt';fctx.beginPath();fctx.moveTo(tailStart,e.y);fctx.lineTo(head,e.y);fctx.stroke();fctx.globalAlpha=a*e.intensity;fctx.strokeStyle=g;fctx.lineWidth=e.thickness;fctx.beginPath();fctx.moveTo(tailStart,e.y);fctx.lineTo(head,e.y);fctx.stroke();const headX=e.dir>0?head-e.headLen:head,hg=fctx.createLinearGradient(headX,0,headX+e.headLen,0);if(e.dir>0){hg.addColorStop(0,'rgba(99,213,208,.12)');hg.addColorStop(.58,e.color);hg.addColorStop(.88,COLORS.tealBright);hg.addColorStop(1,COLORS.tealWhite);}else{hg.addColorStop(0,COLORS.tealWhite);hg.addColorStop(.12,COLORS.tealBright);hg.addColorStop(.42,e.color);hg.addColorStop(1,'rgba(99,213,208,.12)');}fctx.globalAlpha=a*Math.min(1,e.intensity+.15);fctx.fillStyle=hg;fctx.fillRect(headX,e.y-e.thickness/2,e.headLen,e.thickness);const coreW=Math.max(1,Math.min(3.2,e.headLen*.2)),coreX=e.dir>0?head-coreW:head,coreH=Math.max(.45,Math.min(1.2,e.thickness*.68));fctx.globalAlpha=a*Math.min(1,e.intensity+.25);fctx.fillStyle=COLORS.tealWhite;fctx.fillRect(coreX,e.y-coreH/2,coreW,coreH);fctx.restore();}
function drawGoldEvent(e,t,a){const local=(t-e.start)/e.duration;if(local<0||local>1)return;const p=easeInOut(local),center=e.dir>0?-e.glintWidth+p*(cssW+2*e.glintWidth):cssW+e.glintWidth-p*(cssW+2*e.glintWidth),trailEnd=clamp(center,0,cssW);if(e.dir>0)eraseRect(0,e.y-e.thickness/2,trailEnd,e.thickness,1);else eraseRect(trailEnd,e.y-e.thickness/2,cssW-trailEnd,e.thickness,1);const baseW=Math.min(cssW,e.glintWidth*1.55),bx=center-baseW/2;fctx.globalAlpha=a*e.baseAlpha;fctx.fillStyle=COLORS.gold;fctx.fillRect(bx,e.y-e.thickness/2,baseW,e.thickness);const patches=[{o:-.34,w:.18,a:.28},{o:-.13,w:.24,a:.58},{o:.04,w:.12,a:1},{o:.19,w:.20,a:.68},{o:.39,w:.10,a:.34}];for(let i=0;i<patches.length;i++){const q=patches[i],f=.72+.28*Math.sin(t*.010*(i+1)+e.phase+i*.9);fctx.globalAlpha=a*q.a*f;fctx.fillStyle=i===2?COLORS.goldHi:COLORS.gold;const pw=e.glintWidth*q.w;fctx.fillRect(center+e.glintWidth*q.o-pw/2,e.y-e.thickness/2,pw,e.thickness);}fctx.globalAlpha=1;}
function finish(){if(finished)return;finished=true;clearTimeout(watchdog);cancelAnimationFrame(raf);document.documentElement.dataset.entryState='complete';overlay.classList.add('entry-complete');overlay.remove();}
function frame(now){if(!start)start=now;const t=now-start;fctx.clearRect(0,0,cssW,cssH);const a=t<EFFECT_FADE_START?1:1-clamp((t-EFFECT_FADE_START)/(WHITE_GONE_AT-EFFECT_FADE_START),0,1);for(const e of goldEvents)drawGoldEvent(e,t,a);for(const e of tealEvents)drawTealEvent(e,t,a);if(t>=WHITE_GONE_AT){fctx.clearRect(0,0,cssW,cssH);const p=clamp((t-WHITE_GONE_AT)/(TOTAL_MS-WHITE_GONE_AT),0,1);tealLayer.style.opacity=String(1-easeInOut(p));}if(t>=TOTAL_MS){finish();return;}raf=requestAnimationFrame(frame);}
let resizeTimer=0;addEventListener('resize',()=>{clearTimeout(resizeTimer);resizeTimer=setTimeout(()=>{if(!finished){resize();start=performance.now();}},100);},{passive:true});addEventListener('pageshow',e=>{if(e.persisted)finish();},{passive:true});
try{resize();document.documentElement.dataset.entryState='running';watchdog=setTimeout(finish,FAILSAFE_MS);raf=requestAnimationFrame(frame);}catch(error){console.error('Entry overlay failed safely:',error);finishImmediately();}
})();